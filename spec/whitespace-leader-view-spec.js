'use babel';

import WhitespaceLeaderView from '../lib/whitespace-leader-view';

describe('WhitespaceLeaderView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
